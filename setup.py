from setuptools import setup,find_packages
setup(name="seduction",version="2.0.0",author="bad-antics",description="Baudrillard's Seduction - reversibility and appearance analysis",packages=find_packages(where="src"),package_dir={"":"src"},python_requires=">=3.8")
